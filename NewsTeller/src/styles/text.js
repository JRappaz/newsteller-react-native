import {Platform} from 'react-native';

export const font = Platform.OS == 'ios' ? 'Avenir' : 'serif';

export const extraLargeSize = 30;
export const largeSize = 16;
export const midSize = 14;
export const smallSize = 12;
